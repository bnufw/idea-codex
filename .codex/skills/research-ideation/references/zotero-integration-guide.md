# Local Corpus Guide

当前 `idea-codex` 的默认 ideation workflow 不使用 Zotero MCP。

请把 `papers/**/*.md` 视为本项目的默认论文语料层。

建议约定每篇本地笔记至少包含：

- 论文标题
- 作者或第一作者
- 年份与 venue（若已知）
- 问题定义
- 核心方法
- 关键假设
- 结果或结论摘要
- 局限与风险

若某篇笔记缺字段：

- 可以继续用于启发式阅读
- 不能直接当作强证据支撑 novelty 或 claim

若后续确实需要联网核实，应把它明确写成后置独立阶段，而不是默认文献入口。
